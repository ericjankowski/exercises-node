use `node hello.js` to start the server

use `node debug hello.js` to debug the server, but that's deprecated

use `node --inspect --debug-brk` to use Chrome's built-in debugger
  in a chrome window start at about:inspect, then click "Open dedicated DevTools for Node"